import { Component } from 'react'
import {
  View,
  Text
} from 'react-native'

/**
 * the purpose of this component is to have a one button access to all features of
 * the Project xBand client -->
 *   - voice (groups, individually)
 *   - messaging (groups, individually)
 *   - bots/internet acces
 *   - other future features
 */

export default class NewButton extends Component {
  static propTypes = {
    // what to do here?
  }


}
